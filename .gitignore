# --- secrets & local-only context (never commit) ---
.env
.env.*
CLAUDE.local.md
docker-compose.override.yml

# --- data (stays out of the public repo) ---
# Scoped to the deploy runtime bind-mount specifically -- a bare `data/`
# matches at any depth and was silently excluding the *intentionally*
# committed web/src/data/geometry.json (M4's bundled static geometry file).
/deploy/data/
*.db
*.db-*
spike/captures/
*.pb
*.ndjson
*.zip

# --- python ---
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# --- misc ---
.DS_Store
node_modules/
coverage/
web/dist/
