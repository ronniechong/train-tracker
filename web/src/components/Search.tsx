import { Section, Placeholder } from './Section'

// Stub -- real logic (typeahead against the bundled station list, flies
// the map to the result) is M4 Stage 4 remainder, sequenced after 3b.
export function Search() {
  return (
    <Section title="Search">
      <Placeholder>Jump to a station — Stage 4</Placeholder>
    </Section>
  )
}
